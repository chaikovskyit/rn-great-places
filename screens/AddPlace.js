import { View, Text, StyleSheet } from "react-native";

const AddPlace = () => {
  return (
    <View>
      <Text>Add Place</Text>
    </View>
  );
};

export default AddPlace;

const styles = StyleSheet.create({});
